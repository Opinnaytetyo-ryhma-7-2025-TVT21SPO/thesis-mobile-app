name: Build production

  # Ajamalla eas workflow:run *insert tämän tiedoston polku* voidaan tehdä production buildi

jobs:
  android_development_build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: production

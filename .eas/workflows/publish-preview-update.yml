name: Publish preview update
on:
  push:
    branches: ['*']
  # Generoi esikatselun puskusta mille tahansa haaralle.
jobs:
  publish_preview_update:
    name: Publish preview update
    type: update
    params:
      branch: ${{ github.ref || 'test' }}
      